import React from 'react';
import { Outlet } from 'react-router-dom';

const LayoutAdmin = () => {
  return (
    <div>
      LAYOUTADMIN
      <Outlet/>
    </div>
  );
};

export default LayoutAdmin;
